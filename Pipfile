[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = ">=2.0.0"
python-dotenv = ">=1.0.0"
requests = "*"

[dev-packages]
pytest = ">=7.0.0"
pytest-mock = ">=3.10.0"

[requires]
python_version = "3.13"
